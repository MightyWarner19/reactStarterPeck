// src/App.js
import React from 'react';

// import JobCategory from './Components/JobCategory';
function Jobs() {
  return (
    
      <>
     {/* <div className="App">
        <h1 className="text-4xl font-semibold mb-6">Job Application Website</h1>
        <Routes>
          <Route path="/" >
            {jobData.categories.map(category => (
              <JobCategory key={category.id} category={category} />
            ))}
          </Route>
          <Route path="/job/:jobId" exact>
            <JobDetail job={jobData.categories.flatMap(category => category.jobs)} />
          </Route>
        </Routes>
      </div> */}
      </>
  );
}

export default Jobs;
